spring.application.name=convergeshop

spring.datasource.url=jdbc:mysql://${APP_MYSQL_HOST}:${APP_MYSQL_PORT}/${APP_MYSQL_DB}?useSSL=false
spring.datasource.username=${APP_MYSQL_USER_NAME}
spring.datasource.password=${APP_MYSQL_PASSWORD}
## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect
# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update
logging.level.org.hibernate = ERROR
spring.jpa.hibernate.naming.physical-strategy = org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.hibernate.naming.implicit-strategy = org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl



spring.thymeleaf.encoding=UTF-8
# B? t�nh n?ng cache c?a thymeleaf ?? l?p tr�nh cho nhanh
spring.thymeleaf.cache=false
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML5
# C�c message t?nh s? ???c l?u t?i th? m?c i18n
spring.messages.basename=i18n/messages
# B? properties n�y ?i khi deploy
# N� c� t�c d?ng c? ??nh ng�n ng? hi?n t?i ch? l� Ti?ng Vi?t
spring.web.locale-resolver=fixed
# M?c ??nh ng�n ng? l� ti?ng vi?t
spring.web.locale=vi_VN
# ??i th�nh ti?ng anh b?ng c�ch b? comment ? d?oi
#spring.web.locale=en_US

spring.mvc.throw-exception-if-no-handler-found=true
spring.mvc.static-path-pattern=/**
spring.thymeleaf.prefix=classpath:/templates/

spring.main.allow-bean-definition-overriding=true

spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.context.PropertyPlaceholderAutoConfiguration.propertySourcesPlaceholderConfigure

# upload file
file.user-upload-dir=src/main/resources/media/images/user
file.product-upload-dir=src/main/resources/media/images/product
file.sale-upload-dir=src/main/resources/media/images/sale

# Email #${APP_MAIL_HOST}
spring.mail.host=${APP_EMAIL_HOST}
spring.mail.port=${APP_EMAIL_PORT}
spring.mail.username=${APP_EMAIL_USER_NAME}
spring.mail.password=${APP_EMAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# SMS of twilio #${APP_TWILLIO_xxx}
twilio.accountSid=${APP_TWILIO_ACCOUNT_SID}
twilio.authToken=${APP_TWILIO_AUTH_TOKEN}
twilio.serviceSid=${APP_TWILIO_SERVICE_SID}
twilio.phoneNumber=${APP_TWILIO_PHONE_NUMBER}


